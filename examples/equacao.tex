% Equações numeradas, alinhadas e referenciáveis.
% Requer: \usepackage{amsmath} \usepackage{amssymb}

% Equação simples, numerada:
\begin{equation}
  E = mc^2
  \label{eq:einstein}
\end{equation}

% Sistema de equações alinhadas:
\begin{align}
  f(x) &= ax^2 + bx + c \label{eq:quad} \\
  f'(x) &= 2ax + b
\end{align}

% Equação inline: $\sum_{i=1}^{n} x_i$
% Refira como: pela Equação~\ref{eq:einstein}, ...
