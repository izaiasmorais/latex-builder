\section{Interfaces e Endpoints}

Documente as interfaces expostas pela solução: rotas, parâmetros de entrada,
formatos de resposta e códigos de status. Para exemplos de requisição e
resposta, o ambiente \texttt{lstlisting} com a linguagem \texttt{json} já está
configurado no preâmbulo de \texttt{main.tex}.
